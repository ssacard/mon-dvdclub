= render :partial => '/layouts/menu', :locals => {:parent => :admin} if logged_in?
.site
  %h4
    Infos Club
  .data
    %label
      Emails des autres membres du club
    %br
    = text_area_tag :emails, @dvd_club_users.join( "\n" )