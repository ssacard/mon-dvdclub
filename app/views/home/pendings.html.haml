= render :partial => '/layouts/menu', :locals => {:object => :pending, :parent => :myself} if logged_in?

.main-box
  .easyfix
    .main
      %h2{}=t 'requests.incoming' 
      = render :partial => 'dvds/partials/requests', :locals => {:dvds => @dvds}

      %h2{}=t 'requests.sent'
      = render :partial => 'dvds/partials/requests', :locals => {:dvds => @pending_dvds}