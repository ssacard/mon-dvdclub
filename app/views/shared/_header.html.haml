#header.basic
  .site
    .logo
      = link_to 'header.logout.title'[:'header.logout.title'], root_path
    / = Dvd.count
    / DVD à voir gratuitement
    .actions
      - if logged_in?
        = link_to 'Mon Compte', settings_path
        |
        = link_to "Se deconnecter", logout_path
      - else 
        = link_to "header.logout.create"[:'header.logout.create'], register_path
        |  
        = link_to "header.logout.connect"[:'header.logout.connect'], login_path
      
