= render :partial => '/layouts/menu', :locals => {:parent => :myself, :object => :clubs} if logged_in?

.main-box
  .promo-box
    .btn
      .bg-left
        %a{ :href => "/dvd_clubs/new" }
          %span
            %span{}=t 'home.createnew'

    %p{}=t 'home.banner'

  .easyfix.top-border
    .main
      = render :partial => 'dvd_club', :collection => self.current_user.dvd_clubs, :locals => {:all_members => false}
